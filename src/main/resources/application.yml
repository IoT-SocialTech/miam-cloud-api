spring:
  application:
    name: mian-cloud-api
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/miam_cloud_api?useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
    driverClassName: 'com.mysql.cj.jdbc.Driver'
    username: root
    password: UPCsql1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080